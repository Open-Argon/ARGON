const password = input('set password: ')
var guess

while (password != guess) [
    guess = input('password: ')
    if (password != guess) [
        log('wrong password')
    ]
]

log('correct password')